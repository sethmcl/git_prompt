# Colorful git prompt with ZSH

```
# clone project
git clone https://github.com/sethmcl/git_prompt.git ~/git_prompt

# run setup script
~/git_prompt/setup.sh

# install fonts
open ~/git_prompt/fonts # double click each font file then click install within the OSX font manager

# restart terminal
```

